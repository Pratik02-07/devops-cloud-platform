apiVersion: v1
kind: Service

metadata:
  name: devops-api
  namespace: devops-platform

spec:
  type: LoadBalancer

  selector:
    app: devops-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

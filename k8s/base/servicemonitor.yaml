apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: devops-api
  namespace: devops-platform

spec:
  selector:
    matchLabels:
      app: devops-api

  endpoints:
    - port: http
      path: /metrics
      interval: 15s

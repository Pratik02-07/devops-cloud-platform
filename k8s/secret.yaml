apiVersion: v1
kind: Secret

metadata:
  name: devops-api-secrets
  namespace: devops-platform

type: Opaque

# stringData is automatically base64-encoded by Kubernetes.
# Placeholder DATABASE_URL — app will start and /health responds.
# Replace with real Amazon RDS URL in Phase 7/8.
stringData:
  DATABASE_URL: "mysql://devops:devops@placeholder-rds.ap-south-1.rds.amazonaws.com:3306/devops"
